---
meta:
  environment: node
  toolchain:
    name: npm
    version: 18
build:
  skip: true
  artifacts:
    '*': /
run:
  scriptName: src/index.ts
  command: npx ts-node src/index.ts
  persistenceMount: /data
  containerPort: 3000
